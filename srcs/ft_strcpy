char *ft_strcpy(char *destination, const char *source)
{
    char *start;

    start = destination;
    while (*source != '\0')
    {
        *destination = *source;
        destination++;
        source++;
    }
    *destination = '\0'; 
    return start;
}
